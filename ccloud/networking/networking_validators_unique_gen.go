// Code generated by ogen, DO NOT EDIT.

package networking

import (
	"github.com/ogen-go/ogen/validate"
)

// validateUniqueError checks for duplicate items using hash-based detection.
func validateUniqueError(items []Error) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  Error
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueErrorSource checks for duplicate items using hash-based detection.
func validateUniqueErrorSource(items []ErrorSource) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ErrorSource
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1ZoneInfo checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1ZoneInfo(items []NetworkingV1ZoneInfo) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1ZoneInfo
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1AccessPointsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1AccessPointsOKDataItem(items []ListNetworkingV1AccessPointsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1AccessPointsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1AccessPointsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1AccessPointsOKDataItemSpec(items []ListNetworkingV1AccessPointsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1AccessPointsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1AccessPointsOKDataItemSpecConfig checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1AccessPointsOKDataItemSpecConfig(items []ListNetworkingV1AccessPointsOKDataItemSpecConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1AccessPointsOKDataItemSpecConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueObjectReference checks for duplicate items using hash-based detection.
func validateUniqueObjectReference(items []ObjectReference) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ObjectReference
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1AccessPointStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1AccessPointStatus(items []NetworkingV1AccessPointStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1AccessPointStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1AccessPointStatusConfig checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1AccessPointStatusConfig(items []NetworkingV1AccessPointStatusConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1AccessPointStatusConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsForwardersOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsForwardersOKDataItem(items []ListNetworkingV1DnsForwardersOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsForwardersOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsForwardersOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsForwardersOKDataItemSpec(items []ListNetworkingV1DnsForwardersOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsForwardersOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsForwardersOKDataItemSpecConfig checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsForwardersOKDataItemSpecConfig(items []ListNetworkingV1DnsForwardersOKDataItemSpecConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsForwardersOKDataItemSpecConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1DnsForwarderStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1DnsForwarderStatus(items []NetworkingV1DnsForwarderStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1DnsForwarderStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsRecordsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsRecordsOKDataItem(items []ListNetworkingV1DnsRecordsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsRecordsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsRecordsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsRecordsOKDataItemSpec(items []ListNetworkingV1DnsRecordsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsRecordsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1DnsRecordsOKDataItemSpecConfig checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1DnsRecordsOKDataItemSpecConfig(items []ListNetworkingV1DnsRecordsOKDataItemSpecConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1DnsRecordsOKDataItemSpecConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueTypedEnvScopedObjectReference checks for duplicate items using hash-based detection.
func validateUniqueTypedEnvScopedObjectReference(items []TypedEnvScopedObjectReference) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  TypedEnvScopedObjectReference
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1DnsRecordStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1DnsRecordStatus(items []NetworkingV1DnsRecordStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1DnsRecordStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1GatewaysOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1GatewaysOKDataItem(items []ListNetworkingV1GatewaysOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1GatewaysOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1GatewaysOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1GatewaysOKDataItemSpec(items []ListNetworkingV1GatewaysOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1GatewaysOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1GatewaysOKDataItemSpecConfig checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1GatewaysOKDataItemSpecConfig(items []ListNetworkingV1GatewaysOKDataItemSpecConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1GatewaysOKDataItemSpecConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1GatewayStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1GatewayStatus(items []NetworkingV1GatewayStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1GatewayStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1GatewayStatusCloudGateway checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1GatewayStatusCloudGateway(items []NetworkingV1GatewayStatusCloudGateway) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1GatewayStatusCloudGateway
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkEndpointsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkEndpointsOKDataItem(items []ListNetworkingV1NetworkLinkEndpointsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkEndpointsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkEndpointsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkEndpointsOKDataItemSpec(items []ListNetworkingV1NetworkLinkEndpointsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkEndpointsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueGlobalObjectReference checks for duplicate items using hash-based detection.
func validateUniqueGlobalObjectReference(items []GlobalObjectReference) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  GlobalObjectReference
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueEnvScopedObjectReference checks for duplicate items using hash-based detection.
func validateUniqueEnvScopedObjectReference(items []EnvScopedObjectReference) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  EnvScopedObjectReference
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkLinkEndpointStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkLinkEndpointStatus(items []NetworkingV1NetworkLinkEndpointStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkLinkEndpointStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkServiceAssociationsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkServiceAssociationsOKDataItem(items []ListNetworkingV1NetworkLinkServiceAssociationsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkServiceAssociationsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkServiceAssociationsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkServiceAssociationsOKDataItemSpec(items []ListNetworkingV1NetworkLinkServiceAssociationsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkServiceAssociationsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkLinkServiceAssociationStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkLinkServiceAssociationStatus(items []NetworkingV1NetworkLinkServiceAssociationStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkLinkServiceAssociationStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkServicesOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkServicesOKDataItem(items []ListNetworkingV1NetworkLinkServicesOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkServicesOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworkLinkServicesOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworkLinkServicesOKDataItemSpec(items []ListNetworkingV1NetworkLinkServicesOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworkLinkServicesOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkLinkServiceAcceptPolicy checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkLinkServiceAcceptPolicy(items []NetworkingV1NetworkLinkServiceAcceptPolicy) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkLinkServiceAcceptPolicy
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkLinkServiceStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkLinkServiceStatus(items []NetworkingV1NetworkLinkServiceStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkLinkServiceStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworksOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworksOKDataItem(items []ListNetworkingV1NetworksOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworksOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1NetworksOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1NetworksOKDataItemSpec(items []ListNetworkingV1NetworksOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1NetworksOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1DnsConfig checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1DnsConfig(items []NetworkingV1DnsConfig) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1DnsConfig
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkStatus(items []NetworkingV1NetworkStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1NetworkStatusCloud checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1NetworkStatusCloud(items []NetworkingV1NetworkStatusCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1NetworkStatusCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PeeringsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PeeringsOKDataItem(items []ListNetworkingV1PeeringsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PeeringsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PeeringsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PeeringsOKDataItemSpec(items []ListNetworkingV1PeeringsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PeeringsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PeeringsOKDataItemSpecCloud checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PeeringsOKDataItemSpecCloud(items []ListNetworkingV1PeeringsOKDataItemSpecCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PeeringsOKDataItemSpecCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PeeringStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PeeringStatus(items []NetworkingV1PeeringStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PeeringStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItem(items []ListNetworkingV1PrivateLinkAccessesOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAccessesOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItemSpec(items []ListNetworkingV1PrivateLinkAccessesOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAccessesOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItemSpecCloud checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAccessesOKDataItemSpecCloud(items []ListNetworkingV1PrivateLinkAccessesOKDataItemSpecCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAccessesOKDataItemSpecCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PrivateLinkAccessStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PrivateLinkAccessStatus(items []NetworkingV1PrivateLinkAccessStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PrivateLinkAccessStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItem(items []ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpec(items []ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpecCloud checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpecCloud(items []ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpecCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAttachmentConnectionsOKDataItemSpecCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PrivateLinkAttachmentConnectionStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PrivateLinkAttachmentConnectionStatus(items []NetworkingV1PrivateLinkAttachmentConnectionStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PrivateLinkAttachmentConnectionStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PrivateLinkAttachmentConnectionStatusCloud checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PrivateLinkAttachmentConnectionStatusCloud(items []NetworkingV1PrivateLinkAttachmentConnectionStatusCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PrivateLinkAttachmentConnectionStatusCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAttachmentsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAttachmentsOKDataItem(items []ListNetworkingV1PrivateLinkAttachmentsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAttachmentsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1PrivateLinkAttachmentsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1PrivateLinkAttachmentsOKDataItemSpec(items []ListNetworkingV1PrivateLinkAttachmentsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1PrivateLinkAttachmentsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PrivateLinkAttachmentStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PrivateLinkAttachmentStatus(items []NetworkingV1PrivateLinkAttachmentStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PrivateLinkAttachmentStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1PrivateLinkAttachmentStatusCloud checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1PrivateLinkAttachmentStatusCloud(items []NetworkingV1PrivateLinkAttachmentStatusCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1PrivateLinkAttachmentStatusCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItem checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItem(items []ListNetworkingV1TransitGatewayAttachmentsOKDataItem) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1TransitGatewayAttachmentsOKDataItem
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItemSpec checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItemSpec(items []ListNetworkingV1TransitGatewayAttachmentsOKDataItemSpec) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1TransitGatewayAttachmentsOKDataItemSpec
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItemSpecCloud checks for duplicate items using hash-based detection.
func validateUniqueListNetworkingV1TransitGatewayAttachmentsOKDataItemSpecCloud(items []ListNetworkingV1TransitGatewayAttachmentsOKDataItemSpecCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  ListNetworkingV1TransitGatewayAttachmentsOKDataItemSpecCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1TransitGatewayAttachmentStatus checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1TransitGatewayAttachmentStatus(items []NetworkingV1TransitGatewayAttachmentStatus) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1TransitGatewayAttachmentStatus
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}

// validateUniqueNetworkingV1TransitGatewayAttachmentStatusCloud checks for duplicate items using hash-based detection.
func validateUniqueNetworkingV1TransitGatewayAttachmentStatusCloud(items []NetworkingV1TransitGatewayAttachmentStatusCloud) (err error) {
	if len(items) <= 1 {
		return nil
	}

	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*validate.DepthLimitError); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()

	type entry struct {
		item  NetworkingV1TransitGatewayAttachmentStatusCloud
		index int
	}
	buckets := make(map[uint64][]entry, len(items))

	for i, item := range items {
		hash := item.Hash()
		bucket := buckets[hash]

		for _, existing := range bucket {
			if item.Equal(existing.item, 0) {
				return &validate.DuplicateItemsError{
					Indices: []int{existing.index, i},
				}
			}
		}

		buckets[hash] = append(bucket, entry{item: item, index: i})
	}

	return nil
}
